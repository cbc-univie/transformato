name: test
channels:
  - conda-forge
  - omnia/label/dev
  - omnia
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - openmm
  - numpy
  - pymbar
  - rdkit
  - parmed

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov


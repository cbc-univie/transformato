name: test
channels:
  - conda-forge
  - omnia/label/dev
  - omnia
  - defaults
dependencies:
    # Base depends
  - python>=3.7
  - pip
  - openmm
  - numpy
  - pymbar
  - rdkit
  - parmed
  - pyyaml
  - ipython
  - jupyter
  - mdtraj
  - matplotlib
  - networkx
  - tqdm

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

